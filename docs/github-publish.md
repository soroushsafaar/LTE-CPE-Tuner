# Publish to GitHub

```bash
git init
git add .
git commit -m "Initial LTE CPE tuner project"
git branch -M main
git remote add origin https://github.com/YOUR_USER/lte-cpe-tuner.git
git push -u origin main
```

Recommended repository description:

```text
Browser-console toolkit for LTE/TD-LTE CPE SINR optimization, band/cell tuning, WAN status parsing, angle scans, and CSV logging.
```

Recommended topics:

```text
lte, td-lte, cpe, sinr, rsrq, band-lock, cell-lock, router, javascript
```
